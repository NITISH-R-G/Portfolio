# src/registry/transformed/ is generated from src/registry/components/ by registry:build and
# committed, so it duplicates its source by construction. Excluded from duplication only.
sonar.cpd.exclusions=src/registry/transformed/**
