/**
 * Content for this portfolio comes from the engine, not from a hand-written file.
 *
 * Upstream (chanhdai.com) this module held his personal data as a literal. Here it is a
 * re-export from `./adapter`, which maps our normalized profile — the output of the connector
 * and normalization pipeline — onto the same exported names and the same types. His components
 * import from this path unchanged.
 *
 * Edit the portfolio through the admin, or re-run `npm run import`. Do not hand-edit this file.
 */

export { TECH_STACK } from './adapter'
