# Analytics (optional)
# Set this to enable remote event logging via sendBeacon.
# Accepts any POST endpoint that accepts JSON: { events: [...] }
# Examples: webhook, Umami API, Plausible API, custom backend
# VITE_ANALYTICS_ENDPOINT=https://your-endpoint.example.com/event
